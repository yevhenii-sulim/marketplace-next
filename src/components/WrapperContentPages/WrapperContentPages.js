import React from 'react';
import { Wrapper } from './WrapperContentPages.styled';

function WrapperContentPages({ children }) {
  return <Wrapper>{children}</Wrapper>;
}

export default WrapperContentPages;
